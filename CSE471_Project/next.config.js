/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Remove turbo option as it's not valid
  },
  // Remove eslint config as it's not supported in this format
};

module.exports = nextConfig;